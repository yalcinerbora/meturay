#include <gtest/gtest.h>
#include <gmock/gmock.h>

#include "RayLib/Ray.h"


TEST(RayCPU, AABBIntersection)
{
    //RayF r = RayF(Vector3f(0.5, 0.5, 0.5),
    //              Vector3f(0.5, 0.5, 0.5).NormalizeSelf());

    //Vector3f aabbMin(-1, -1, -1);
    //Vector3f aabbMax( 1,  1,  1);
    //Vector2f tMinMax = Vector2f(0.01f, FLT_MAX);

    //float tMin;
    //Vector3f pos;
    //bool doIntersect = r.IntersectsAABB(pos, tMin, aabbMin, aabbMax,
    //                                    tMinMax);



}